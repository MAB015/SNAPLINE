# contracts

Solidity + Hardhat 3 + viem. Se crea en D1.

Ver `docs/ARCHITECTURE.md` seccion 2 para el diseno de `SplitPool`,
`SplitPoolFactory` y `MockUSDT`, y `docs/THREAT-MODEL.md` seccion 6 para la
lista de tests que deben pasar.
