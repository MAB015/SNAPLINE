# deployments

Direcciones desplegadas por cadena. `hashkey-testnet.json` se genera en D2 con el
comando `forge create` e incluye las direcciones del factory, la implementacion
de referencia y MockUSDT, con su bloque de despliegue.
