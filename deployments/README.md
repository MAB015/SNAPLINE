# deployments

Direcciones desplegadas por cadena. `base-sepolia.json` se genera en D2 con el
script de despliegue e incluye las direcciones del factory, la implementacion
de referencia y MockUSDT, con su bloque de despliegue.
